name: Push Events
on: 
  push:
    branches:
      - main
      - test
jobs:
  show-info:
      runs-on: ubuntu-latest
      steps:
      - name: info
        run: |
          echo "this is push event on all the branches"
        
          
  
